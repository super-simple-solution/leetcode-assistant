import { Plugin } from "rollup";
export declare type SimpleReloaderPlugin = Pick<Required<Plugin>, "name" | "generateBundle" | "writeBundle">;
export interface SimpleReloaderOptions {
    executeScript?: boolean;
    unregisterServiceWorkers?: boolean;
}
export interface SimpleReloaderCache {
    bgScriptPath?: string;
    ctScriptPath?: string;
    timestampPath?: string;
    outputDir?: string;
    loadMessage?: string;
}
export declare const _internalCache: SimpleReloaderCache;
export declare const simpleReloader: ({ executeScript, unregisterServiceWorkers, }?: SimpleReloaderOptions, cache?: SimpleReloaderCache) => SimpleReloaderPlugin | undefined;
