import { OutputBundle, PluginContext } from "rollup";
import { ChromeExtensionManifest } from "../../manifest";
import { NormalizedChromeExtensionOptions } from "../../plugin-options";
export declare class ContentScriptProcessor {
    private options;
    constructor(options: NormalizedChromeExtensionOptions);
    generateBundle(context: PluginContext, bundle: OutputBundle, manifest: ChromeExtensionManifest): Promise<void>;
}
