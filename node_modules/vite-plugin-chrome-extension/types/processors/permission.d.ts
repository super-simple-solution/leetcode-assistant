import { OutputChunk, PluginContext } from "rollup";
import { OutputChunkBundle } from "../common/models";
import { ChromeExtensionManifest } from "../manifest";
export declare const derivePermissions: (set: Set<string>, { code }: OutputChunk) => Set<string>;
export declare class PermissionProcessorCache {
    permsHash: string;
    assetChanged: boolean;
}
export declare class PermissionProcessorOptions {
    verbose: boolean;
}
export declare class PermissionProcessor {
    private options;
    private cache;
    constructor(options: PermissionProcessorOptions);
    derivePermissions(context: PluginContext, chunks: OutputChunkBundle, manifest: ChromeExtensionManifest): void;
}
