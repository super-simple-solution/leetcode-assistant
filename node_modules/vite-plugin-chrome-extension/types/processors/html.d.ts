import { InputOption, OutputBundle, PluginContext } from "rollup";
import { NormalizedChromeExtensionOptions } from "../plugin-options";
export declare class HtmlProcessor {
    private options;
    private cache;
    constructor(options: NormalizedChromeExtensionOptions);
    resolveInput(input?: InputOption): string | string[] | {
        [x: string]: string;
    } | undefined;
    addWatchFiles(context: PluginContext): void;
    generateBundle(context: PluginContext, bundle: OutputBundle): void;
    /**
     * Output asset files in html
     * css, img, script(not local import)
     */
    emitFiles(context: PluginContext): Promise<void>;
    clearCacheById(id: string): void;
    private replaceImportScriptPath;
}
