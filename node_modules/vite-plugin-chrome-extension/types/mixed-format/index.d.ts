import { Plugin } from "rollup";
import { ManifestInputPlugin } from "../plugin-options";
export declare function mixedFormat(options: Pick<ManifestInputPlugin, "formatMap">): Pick<Required<Plugin>, "name" | "generateBundle">;
