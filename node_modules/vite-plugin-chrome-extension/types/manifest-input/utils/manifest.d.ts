import { ChromeExtensionManifest } from "../../manifest";
export declare function replaceServiceWorkerPath(manifest: ChromeExtensionManifest, replace: (path: string) => string): ChromeExtensionManifest;
export declare function replaceContentScriptsPath(manifest: ChromeExtensionManifest, replace: (path: string) => string): ChromeExtensionManifest;
